[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    llm: Tests die einen echten LLM-Call benötigen (langsam)
    slow: Langsame Tests (>10s)

# Timeouts
timeout = 120

# Output
addopts = -v --tb=short --no-header -q
